using System.Activities.Presentation;

namespace TextRecognizers.Sequences.Design
{
    /// <summary>
    /// Designer for <c>ParseSequence</c>. Supplies the link icon; the inputs are edited in the
    /// property panel (their drop-downs and tooltips come from the activity itself).
    /// </summary>
    public partial class ParseSequenceDesigner : ActivityDesigner
    {
        public ParseSequenceDesigner()
        {
            InitializeComponent();
        }
    }
}
