using System.Activities.Presentation;

namespace TextRecognizers.Choices.Design
{
    /// <summary>
    /// Designer for <c>ParseBoolean</c>. Supplies the check icon; the inputs are edited in the
    /// property panel (their drop-downs and tooltips come from the activity itself).
    /// </summary>
    public partial class ParseBooleanDesigner : ActivityDesigner
    {
        public ParseBooleanDesigner()
        {
            InitializeComponent();
        }
    }
}
