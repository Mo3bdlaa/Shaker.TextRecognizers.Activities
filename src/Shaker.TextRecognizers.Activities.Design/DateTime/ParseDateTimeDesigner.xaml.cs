using System.Activities.Presentation;

namespace TextRecognizers.DateTimes.Design
{
    /// <summary>
    /// Designer for <c>ParseDateTime</c>. Supplies the clock icon; the inputs are edited in the
    /// property panel (their drop-downs and tooltips come from the activity itself).
    /// </summary>
    public partial class ParseDateTimeDesigner : ActivityDesigner
    {
        public ParseDateTimeDesigner()
        {
            InitializeComponent();
        }
    }
}
