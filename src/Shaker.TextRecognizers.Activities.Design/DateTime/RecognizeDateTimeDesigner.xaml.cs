using System.Activities.Presentation;

namespace TextRecognizers.DateTimes.Design
{
    /// <summary>
    /// Designer for <c>RecognizeDateTime</c>. Supplies the calendar icon; the inputs are edited in the
    /// property panel (their drop-downs and tooltips come from the activity itself).
    /// </summary>
    public partial class RecognizeDateTimeDesigner : ActivityDesigner
    {
        public RecognizeDateTimeDesigner()
        {
            InitializeComponent();
        }
    }
}
