using System.Activities.Presentation;

namespace TextRecognizers.Units.Design
{
    /// <summary>
    /// Designer for <c>RecognizeMeasurements</c>. Supplies the ruler icon; the inputs are edited in the
    /// property panel (their drop-downs and tooltips come from the activity itself).
    /// </summary>
    public partial class RecognizeMeasurementsDesigner : ActivityDesigner
    {
        public RecognizeMeasurementsDesigner()
        {
            InitializeComponent();
        }
    }
}
