using System.Activities.Presentation;

namespace TextRecognizers.Numbers.Design
{
    /// <summary>
    /// Designer for <c>ParseNumber</c>. Supplies the numeral one icon; the inputs are edited in the
    /// property panel (their drop-downs and tooltips come from the activity itself).
    /// </summary>
    public partial class ParseNumberDesigner : ActivityDesigner
    {
        public ParseNumberDesigner()
        {
            InitializeComponent();
        }
    }
}
