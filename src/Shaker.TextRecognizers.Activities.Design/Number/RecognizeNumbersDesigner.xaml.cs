using System.Activities.Presentation;

namespace TextRecognizers.Numbers.Design
{
    /// <summary>
    /// Designer for <c>RecognizeNumbers</c>. Supplies the hash icon; the inputs are edited in the
    /// property panel (their drop-downs and tooltips come from the activity itself).
    /// </summary>
    public partial class RecognizeNumbersDesigner : ActivityDesigner
    {
        public RecognizeNumbersDesigner()
        {
            InitializeComponent();
        }
    }
}
