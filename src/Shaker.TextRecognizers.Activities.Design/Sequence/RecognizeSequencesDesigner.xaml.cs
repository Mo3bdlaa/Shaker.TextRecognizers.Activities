using System.Activities.Presentation;

namespace TextRecognizers.Sequences.Design
{
    /// <summary>
    /// Designer for <c>RecognizeSequences</c>. Supplies the envelope icon; the inputs are edited in the
    /// property panel (their drop-downs and tooltips come from the activity itself).
    /// </summary>
    public partial class RecognizeSequencesDesigner : ActivityDesigner
    {
        public RecognizeSequencesDesigner()
        {
            InitializeComponent();
        }
    }
}
